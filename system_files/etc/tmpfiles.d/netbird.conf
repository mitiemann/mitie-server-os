# Create netbird log directory on boot.
# The netbird systemd unit writes to /var/log/netbird/ via StandardOutput/StandardError;
# without this directory the service fails with status=209/STDOUT.
d /var/log/netbird 0755 root root -
