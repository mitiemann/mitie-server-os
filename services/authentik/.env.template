# PostgreSQL password
# Generate with: openssl rand -base64 32
PG_PASS=

# Authentik secret key (Django SECRET_KEY — must be at least 50 chars)
# Generate with: openssl rand -base64 50
AUTHENTIK_SECRET_KEY=
